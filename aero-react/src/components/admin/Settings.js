import React, { Fragment } from "react";

// TODO
// allow reset of "WorkorderId"
// allow setting max appts per day

const Settings = () => {
  return (
    <Fragment>
      <h4>Settings</h4>
    </Fragment>
  );
};

export default Settings;
